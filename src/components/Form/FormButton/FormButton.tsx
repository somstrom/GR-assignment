import React from 'react'

type props = {
    context: string;
    type: string;
}

const FormButton = ( {context, type}: props ) => {
  return (
    <div>FormButton</div>
  )
}

export default FormButton